apiVersion: v1
kind: Service
metadata:
  name: dev-service
  
spec:
  selector:
    app: dev-pod
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
      nodePort: 30010
  type: NodePort
